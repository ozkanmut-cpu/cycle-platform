name: cycle_clinical_domain
description: Clinical domain foundations for condition packs and symptom-first routing.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  cycle_core_domain:
    path: ../core_domain

dev_dependencies:
  test: ^1.25.0
