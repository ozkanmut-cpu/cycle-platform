name: cycle_security
description: Shared app-lock and re-authentication policies for Cycle Platform.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  test: ^1.25.8
