name: cycle_simulation_domain
description: Deterministic simulation primitives for Cycle validation and replay.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  cycle_core_domain:
    path: ../core_domain
  cycle_permissions:
    path: ../permissions
  cycle_sharing:
    path: ../sharing
  cycle_clinical_copilot:
    path: ../clinical_copilot

dev_dependencies:
  test: ^1.25.8
