name: cycle_crypto
description: Cryptographic contracts and key hierarchy for Cycle Platform.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  cryptography: ^2.9.0

dev_dependencies:
  test: ^1.25.8
