name: cycle_clinical_copilot
description: Evidence-linked clinician workflow and safe AI copilot primitives for Cycle.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  cycle_core_domain:
    path: ../core_domain
  cycle_intelligence:
    path: ../intelligence
  cycle_clinical_documents:
    path: ../clinical_documents

dev_dependencies:
  test: ^1.25.8
